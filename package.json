{
    "name": "sls-local-app-demo",
    "description": "sls-local-app-demo",
    "version": "0.0.1",
    "dependencies": {
        "aws-sdk": "^2.799.0",
        "serverless-dynamodb-local": "^0.2.30"
    },
    "devDependencies": {
        "jest": "^26.6.3",
        "serverless-offline": "^12.0.2"
    },
    "scripts": {
        "test": "jest"
    }
}
